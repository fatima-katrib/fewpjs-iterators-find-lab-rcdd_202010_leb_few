const testVar = {}

function superbowlWin = (record) => {
  let result = record.find( record => record.result === "W" )
  return result.year}
  
function testFunc() {

  let result = record.find( record => record.result === "W" )
  return result.year

  return "hi"
}
